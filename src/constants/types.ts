export interface NewPost {
    title: string,
    content: string,
    id: number,
    createdAt: string,
    updatedAt: string
}

export interface Theme {
    id: number,
    name: string,
    mainColor: string,
    secondColor: string,
    title: string,
    textColor: string
}

